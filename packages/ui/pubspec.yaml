name: la10_ui
description: Shared widgets, theme, and design tokens for La 10.
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  la10_core:
    path: ../core

dev_dependencies:
  very_good_analysis: ^6.0.0
