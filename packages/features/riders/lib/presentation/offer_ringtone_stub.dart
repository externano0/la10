/// Implementación no-web del ringtone de oferta.
///
/// En Android/iOS por ahora no reproducimos nada desde Dart — el sistema
/// nativo se encarga via notificación push (futuro). En el ínterin, el
/// popup full-screen sigue apareciendo y la app vibra.

void warmUp() {}

Future<void> play() async {}
