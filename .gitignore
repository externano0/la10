# Env — NUNCA commitear las claves
.env
.env.*
!.env.example

# Vercel CLI state
.vercel/

# Node (Vercel CLI deps si aparece)
node_modules/

# Melos: cada package genera pubspec_overrides.yaml localmente
pubspec_overrides.yaml
**/pubspec_overrides.yaml

# Android signing / build
**/android/.gradle/
**/android/captures/
**/android/local.properties
*.jks
*.keystore
key.properties

# Nota: pubspec.lock SÍ se commitea (para builds reproducibles en CI).

# Dart / Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
*.iml
.idea/
.vscode/
.fvm/

# Codegen output (kept untracked; regenerate via melos gen)
**/*.g.dart
**/*.freezed.dart
**/*.mocks.dart

# Flutter desktop / mobile build dirs
android/.gradle/
android/local.properties
ios/Pods/
ios/.symlinks
ios/Flutter/Flutter.framework
ios/Flutter/Flutter.podspec
macos/Pods/
windows/build/
linux/build/

# Backup of legacy project (kept locally, not pushed)
_backup/
!_backup/**/README.md

# Supabase CLI scratch
supabase/.temp/
supabase/.branches/

# OS
.DS_Store
Thumbs.db

# Claude Code local config (per-developer)
.claude/settings.local.json

# APKs descargados localmente (los release los publica GitHub)
dist/
*.apk
